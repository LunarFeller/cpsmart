# 更新日志

本项目遵循语义化版本号。

## 1.8.0 — 2026-08-26

### 新增

- 快捷键设置窗口：所有快捷键均可录制修改，支持方向键预设、冲突交换、单项与全部恢复默认，重启后保留。
- 收藏板：固定“历史记录”标签与多个自定义收藏板，支持命名、颜色、重命名、删除；历史卡片可拖入收藏板（复制语义），收藏板内可拖动排序；支持“收藏到…”菜单与 `⌘D`。

### 修复

- 修复搜索框聚焦时按空格误触发预览、无法输入多词搜索的问题；即使把空格重绑定给其他动作，搜索框内空格也始终是输入字符。
- 修复新建或重命名收藏板时名称无法输入的问题。
- 修复快速切换预览时轻量预览偶尔退化成大屏 Quick Look 的问题。
- 修复 Quick Look 打开时鼠标点选其他卡片或切换收藏板后预览不同步的问题。
- 修复收藏板删除或窗口重开后搜索框占位文字残留的问题。
- 修复浮窗关闭动画期间全局快捷键被吞掉的问题。

### 改进

- 收藏板标签改为扁平填色样式，悬停有反馈，浅色模式下不再有生硬边框。
- 收藏板颜色改用色点直接选择，右键颜色菜单同步显示色点。
- `⌘D` 收藏菜单改为在选中卡片下方展开。
- 快捷键设置移除方向键预设切换；单项恢复改为文字按钮；修正“已修改”徽标的对齐。

## 1.7.0 — 2026-08-25

### 新增

- 打开历史窗口后默认进入卡片浏览，使用 `Tab` 进入或退出搜索。
- 使用空格键预览文本、图片和文件；文本和图片会按内容自适应尺寸，并可展开到完整 Quick Look。
- 使用 `⌘P` 置顶记录；置顶记录不受普通清空和保留上限影响。
- 显示复制内容的来源应用及图标。
- 增加全部、文本、图片和文件类型筛选。
- 增加浅色、深色和跟随系统外观。

### 改进

- 单击卡片选择并复制，双击卡片直接粘贴；浮窗未激活时首次点击也可生效。
- 修复副屏幕位于主屏幕上方或左侧时，鼠标和触控板无法命中卡片的问题。
- 点击搜索框外返回卡片浏览，搜索为空时空格键始终用于预览。
- 辅助功能权限提示会说明完整的系统设置路径，并可直接打开设置页面。
- “关于 cpsmart”改为更完整的原生使用指南，并统一使用新的陶瓷剪贴板图标。
- 普通清空会保留置顶记录；按住 `⌥` 打开菜单可清空全部记录。
- 安装包脚本从版本文件读取版本号，并区分本地包与正式签名、公证包。

### 移除

- 移除难以理解的应用排除菜单和相关后台逻辑。
