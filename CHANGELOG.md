# 更新日志

本项目遵循语义化版本号。

## 1.9.0 — 2026-08-26

### 新增

- “最近”列表采用 LRU 顺序：通过回车或双击向目标应用发出粘贴后，该记录会提升到当前分组最前并持久化；单击复制、预览、权限不足或目标不可用不改变排序，置顶记录仍保持优先。
- 增加 GitHub Release 更新检测：默认每天自动检查，也可从菜单栏手动检查；发现新版后直接下载并打开官方 Universal DMG。
- 增加“自动检查更新”菜单开关；更新提示会显示在鼠标所在屏幕，并支持负坐标副屏。

### 改进

- 下载更新后的提示改为清晰的三步安装说明，并在自动粘贴失效时引导用户进入权限修复流程。
- 自动粘贴权限失效时提供“重置旧权限并打开设置”：仅清除 cpsmart 自身的旧辅助功能记录，并跳转到系统设置；最终授权开关仍由用户确认。
- 更新下载提示新增“退出 cpsmart”按钮；权限修复也会在打开设置后自动退出，避免模态提示阻挡安装，以及用户在旧进程内反复尝试尚未生效的权限。

## 1.8.0 — 2026-08-26

### 新增

- 快捷键设置窗口：所有快捷键均可录制修改，支持冲突交换、单项与全部恢复默认，重启后保留。
- 收藏板：固定“历史记录”标签与多个自定义收藏板，支持命名、颜色、重命名、删除；历史卡片可拖入收藏板（复制语义），收藏板内可拖动排序；支持“收藏到…”菜单与 `⌘D`。

### 修复

- 修复搜索框聚焦时按空格误触发预览、无法输入多词搜索的问题；即使把空格重绑定给其他动作，搜索框内空格也始终是输入字符。
- 修复新建或重命名收藏板时名称无法输入的问题。
- 修复快速切换预览时轻量预览偶尔退化成大屏 Quick Look 的问题。
- 修复 Quick Look 面板关闭时序问题：`QLPreviewPanel` 不响应 `orderOut`，且关闭过程中弹气泡会把面板复活；现在方向键切换时两种预览不再同时残留。
- 修复快捷键设置滚动时内容压到窗口左上角按钮的问题；修正“已修改”徽标文字偏上。
- 修复 Quick Look 打开时鼠标点选其他卡片或切换收藏板后预览不同步的问题。
- 修复“关于”页在快捷键修改后不同步、GitHub 链接无法点击的问题。
- 修复“关于”页和“快捷键设置”页滚动时内容压到左上角窗口按钮的问题：改用 safeArea 约束，内容在物理上无法进入标题栏区域。
- 修复“关于”页快捷键双栏列表重复显示一行的问题。
- 修复新建收藏板表单缺少名称输入框的问题（色点行改为堆栈布局后输入框没有宽度约束，塌缩成一条缝）。
- 粘贴目标与关闭浮窗的焦点归还跟随用户最近操作的窗口：浮窗打开期间点击了其他应用的窗口后，粘贴会进入最新活跃的应用，而不是浮窗打开瞬间锁定的应用。
- 预览改为会话行为：`Space` 开启后方向键浏览会持续预览文本/图片；切到文件时预览暂时关闭，切回文本/图片自动恢复；再按 `Space` 或 `Esc` 结束预览。
- 修复收藏板删除或窗口重开后搜索框占位文字残留的问题。
- 修复浮窗关闭动画期间全局快捷键被吞掉的问题。

### 改进

- “历史记录”标签更名为“最近”。
- 收藏按钮改为星标图标并常驻右上角，收藏板视图下置灰而不再消失。
- 收藏菜单（`⌘D`）在选中卡片下方展开，带“收藏到收藏板”标题和板颜色色点。
- 新建收藏板的颜色改为一排可直接点选的色点。
- 收藏板标签改为扁平填色样式，色点居中，悬停有反馈，浅色模式下不再有生硬边框。
- 快捷键设置移除方向键预设切换；单项恢复改为文字按钮。
- 收藏板搜索框占位符改为“在「板名」中搜索”，明确搜索范围只覆盖当前板。
- 文件不再提供预览：Quick Look 对多数文件只显示图标，且面板尺寸异步重算会明显闪动；文本和图片仍可展开到完整 Quick Look。

## 1.7.0 — 2026-08-25

### 新增

- 打开历史窗口后默认进入卡片浏览，使用 `Tab` 进入或退出搜索。
- 使用空格键预览文本、图片和文件；文本和图片会按内容自适应尺寸，并可展开到完整 Quick Look。
- 使用 `⌘P` 置顶记录；置顶记录不受普通清空和保留上限影响。
- 显示复制内容的来源应用及图标。
- 增加全部、文本、图片和文件类型筛选。
- 增加浅色、深色和跟随系统外观。

### 改进

- 单击卡片选择并复制，双击卡片直接粘贴；浮窗未激活时首次点击也可生效。
- 修复副屏幕位于主屏幕上方或左侧时，鼠标和触控板无法命中卡片的问题。
- 点击搜索框外返回卡片浏览，搜索为空时空格键始终用于预览。
- 辅助功能权限提示会说明完整的系统设置路径，并可直接打开设置页面。
- “关于 cpsmart”改为更完整的原生使用指南，并统一使用新的陶瓷剪贴板图标。
- 普通清空会保留置顶记录；按住 `⌥` 打开菜单可清空全部记录。
- 安装包脚本从版本文件读取版本号，并区分本地包与正式签名、公证包。

### 移除

- 移除难以理解的应用排除菜单和相关后台逻辑。
