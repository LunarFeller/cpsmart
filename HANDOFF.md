# cpsmart 开发交接文档（Handoff）

> 本文档由 Kimi 编写，用于把「逻辑/数据层」工作交接给 Codex 执行。
> UI/前端（`HistoryWindowController.swift` 的界面重写）由 Kimi 负责，不在本文任务范围内。
> 双方通过第二节的「接口契约」并行开发，互不阻塞。

## 0. 项目背景

cpsmart 是一个原生 macOS 菜单栏剪贴板历史工具（纯 Swift + AppKit，无第三方依赖，SwiftPM 构建）。

- 记录文本 / 图片 / 文件三类剪贴板内容，持久化到 `~/Library/Application Support/cpsmart/history.json`（JSON 内嵌 base64 图片数据，上限 200 条 / 25 MB）。
- `⇧⌘V` 呼出屏幕底部全宽浮窗（`NSPanel`，高 210pt），横向 `NSCollectionView` 卡片流。
- 单击/方向键选择，回车返回原应用并模拟 `⌘V` 粘贴（需辅助功能权限），`Esc` 关闭，`⌫` 删除。
- 已跳过密码管理器标记（Concealed/Transient/AutoGenerated）的内容。

### 文件地图

| 文件 | 职责 | 归属 |
|---|---|---|
| `Sources/cpsmart/HistoryWindowController.swift` | 浮窗 UI 全部（面板、卡片、键盘交互） | **Kimi（不要动）** |
| `Sources/cpsmart/Models.swift` | `ClipboardPayload` / `ClipboardEntry` 数据模型 | Codex |
| `Sources/cpsmart/HistoryStore.swift` | 历史存取、去重、容量裁剪、JSON 持久化 | Codex |
| `Sources/cpsmart/ClipboardMonitor.swift` | 轮询剪贴板、读取/写入 payload | Codex |
| `Sources/cpsmart/PasteController.swift` | 模拟 `⌘V` 粘贴到目标应用 | Codex |
| `Sources/cpsmart/AppDelegate.swift` | 菜单栏、各模块接线 | Codex（接线时保持窗口控制器对外接口兼容，见 §2） |
| `Sources/cpsmart/GlobalHotKey.swift`、`main.swift` | 全局快捷键、入口 | 一般无需改动 |
| `Scripts/core_tests.swift` | 纯逻辑测试（被 `run_tests.sh` 编译执行） | Codex |
| `Scripts/run_tests.sh` / `build_dmg.sh` | 测试 / 出包 | — |

### 构建与验证

```bash
bash Scripts/run_tests.sh    # 纯逻辑单测，必须全绿
swift build                  # 快速编译检查
bash Scripts/build_dmg.sh    # Universal DMG（慢，收尾再跑）
```

UI 冒烟：运行编译出的二进制加 `--show-history` 参数可直接弹出浮窗（无需辅助功能权限），配合 `screencapture` 截图检查界面。

## 1. 本期目标

1. **剪贴板内容搜索**：浮窗内输入即过滤（type-to-search），大小写/变音符号不敏感。
2. **图片缩略图**：图片卡片显示真实缩略图，而不是 24pt 小图标。
3. **来源应用**（顺手做）：卡片显示内容来自哪个 App（名称 + 图标），为后续 UI 改版供数据。

## 2. 接口契约（双方遵守，先写定再动工）

Kimi 的 UI 代码只依赖以下两个新模块的接口；Codex 实现这两个模块，**接口签名不要改**。

### 2.1 搜索过滤（新文件 `Sources/cpsmart/SearchFilter.swift`）

```swift
enum SearchFilter {
    /// 在 entries 中按 query 过滤。
    /// - 空 query（或全空白）原样返回 entries。
    /// - query 按空白分词，多个词之间为 AND 关系。
    /// - 匹配目标为 payload.searchableText（Models.swift 已有该属性），
    ///   比较时用 .caseInsensitive + .diacriticInsensitive + .widthInsensitive。
    static func filter(_ entries: [ClipboardEntry], query: String) -> [ClipboardEntry]
}
```

### 2.2 缩略图（新文件 `Sources/cpsmart/ThumbnailProvider.swift`）

```swift
final class ThumbnailProvider {
    init(maxPixelSize: CGFloat = 480)

    /// 仅对 .image 条目有意义；其他类型直接回调 nil。
    /// 后台线程用 ImageIO（CGImageSourceCreateThumbnailAtIndex）解码并降采样，
    /// 结果用 NSCache 按 entry.id 缓存，completion 在主线程回调。
    /// 同一条目重复请求必须命中缓存，不得重复解码。
    func thumbnail(for entry: ClipboardEntry, completion: @escaping (NSImage?) -> Void)

    /// 浮窗关闭时调用，取消队列中未开始的任务（已缓存结果保留）。
    func cancelAll()
}
```

> 为什么需要它：现在卡片 cell 里 `NSImage(data:)` 直接在主线程解码最大 8 MB 的原图，滚动会卡。UI 改版后所有图片一律走 ThumbnailProvider。

### 2.3 窗口控制器对外接口（Kimi 侧会保持/演进，Codex 接线时以仓库最新代码为准）

`AppDelegate` 目前调用的 `show(entries:)`、`refresh(entries:)`、`onChoose/onPaste/onDelete` 保持不变；搜索状态由窗口控制器内部管理（自己调用 `SearchFilter`），`AppDelegate` 无需感知搜索。

## 3. Codex 任务清单

### T1 搜索过滤 `SearchFilter`（P0）

- 按 §2.1 实现，纯函数，无副作用。
- 测试（追加到 `Scripts/core_tests.swift`）：空 query 返回全部；多词 AND；大小写不敏感（"ABC" 匹配 "abc"）；中文匹配；图片条目能被 "图片"/"image" 命中；文件路径可被文件名命中。

### T2 缩略图 `ThumbnailProvider`（P0）

- 按 §2.2 实现。用 `CGImageSourceCreateThumbnailAtIndex` + `kCGImageSourceCreateThumbnailFromImageIfAbsent`（注意：先尝试 `IfAbsent`，失败再 `Always`，避免大图被整图解码——实际上直接用 `kCGImageSourceCreateThumbnailFromImageAlways = true` 更省心，二选一但要在注释里说明理由）。
- `maxPixelSize` 默认 480，缩略图最长边不超过它。
- 测试：用程序生成的 2×2 PNG 数据构造 `.image` 条目，断言回调非 nil 且尺寸 ≤ 480；断言连续两次请求第二次命中缓存（可用计数器或计时粗判）。

### T3 来源应用记录（P1）

- `ClipboardEntry` 增加 `sourceAppName: String?`（或同时加 `sourceAppBundleID: String?`，二选一的话优先 bundleID + name 都存）。
  - **兼容性**：新增 Optional 字段对旧 history.json 解码是安全的（合成 Codable 对 Optional 用 `decodeIfPresent`），但请写一个测试验证「没有该字段的旧 JSON 能正常解码」。
- `ClipboardMonitor.onCapture` 回调里目前只有 payload；改为同时携带捕获时刻的 `NSWorkspace.shared.frontmostApplication`（注意排除 cpsmart 自身）。改动会波及 `AppDelegate.configureClipboardMonitor` 的接线。
- `HistoryStore.add` 签名相应扩展（建议加默认参数 `sourceAppName: String? = nil`，减少调用方改动）。
- 测试：去重逻辑（相同 payload 不同来源应用）是否仍去重——**保持现有行为：按 payload 去重，来源应用取最新一次**。

### T4 测试与文档收尾（P0）

- `bash Scripts/run_tests.sh` 全绿。
- `README.md` 的「功能」一节补充：搜索（输入即过滤）、图片缩略图、来源应用显示。
- 版本号暂不 bump（UI 改版完成后统一发版）。

### 明确不做（本期范围外，避免范围蔓延）

- 收藏/置顶、QuickLook 预览、OCR 图片文字搜索、历史改为独立 blob 文件存储、设置面板、自定义快捷键——已列入路线图（见 §4），本期不动。
- `HistoryWindowController.swift` 的任何改动。

## 4. 功能路线图

- **第 1 轮（已完成）**：搜索过滤、类型筛选（⌘1–4）、图片缩略图、来源应用记录与图标、UI 整体改版（毛玻璃面板 + Theme 调色板）、浅色/深色/跟随系统外观、删除改为 `⌘⌫`。
- **第 2 轮（本文档 §6）**：置顶 pin、历史保留策略机制、QuickLook 预览；图形设置入口尚未实现。
- **第 3 轮（候选）**：图片 OCR 文字搜索（Vision，本地）、跟随前台 App 筛选、自定义全局快捷键、图片落盘为独立文件（解决 history.json base64 膨胀）。
- **明确不做**：iCloud/跨设备同步、AI/MCP 集成、富文本全格式保存、多层 pinboards、shell 文本变换 filter——与「轻量、本地、隐私」的定位冲突或复杂度不成比例。

## 5. 冲突规避规则

1. Codex 不碰 `HistoryWindowController.swift`（UI 唯一归属 Kimi）。
2. 接口签名以本文档契约小节为准，如需变更先回到本文档更新再改代码。
3. 快捷键占用现状：`⌘1–4` 类型筛选、`⌘P` 置顶、`⌘⌫` 删除、`Esc` 清除搜索/关闭（QuickLook 打开时优先关 QL）、`⏎` 粘贴、`Space` QuickLook 预览。新增快捷键不得与这些冲突。
4. **Codable 兼容性红线**：`ClipboardEntry` 新增字段必须是 Optional（或用自定义 `init(from:)` 提供默认值），否则旧 history.json 无法解码。每个新字段都要配一个「旧 JSON 能正常解码」的测试。
5. 完成后 Codex 输出：改动文件清单 + `run_tests.sh` 结果 + 接口一致性确认。

## 6. 第 2 轮任务（竞品调研结论 → 落地任务）

调研对象：Maccy、Paste、Pastebot 3、CleanClip、Raycast、Paste It。结论摘要见 §7。以下任务按价值/成本排序，T5–T7 为纯逻辑层（Codex 独立完成），T8 需要 UI 配合（Kimi 做面板侧，Codex 做数据层）。

**状态（2026-08-25）：T5、T7、T8 已完成；T6 已取消。**
- T5：Codex 完成 `isPinned`/置顶排序/免淘汰/`clear`/`clearAll`/去重继承；Kimi 完成卡片右上角 pin 标记（accent 色 `pin.fill`，置顶时显示）+ `⌘P` 切换 + AppDelegate `onTogglePin` 接线。
- T7：Codex 完成三项保留设置（`maximumEntryCount`/`maximumStorageMB`/`keepDays`，UserDefaults）。设置入口 UI 未做。
- T8：Kimi 完成 Space 触发 `QLPreviewPanel`（文件直接给 URL，图片/文本写 `NSTemporaryDirectory()/cpsmart-quicklook` 临时文件；QL 打开时 ←→ 移动并 reloadData，Space/Esc 关闭，dismiss 时清理）。搜索框聚焦时空格仍留给文本编辑。菜单「清空历史…」注明保留置顶，按住 ⌥ 出现「清空全部历史（含置顶）…」走 `clearAll()`，两者均有 NSAlert 确认。

### T5 置顶 pin（P1，竞品全系标配）

- `ClipboardEntry` 增加 `isPinned: Bool?`（**Optional，见 §5 红线**；nil 视为 false）。
- `HistoryStore`：
  - `togglePin(id:)`；排序规则：置顶条目固定排在未置顶之前，各自组内按时间倒序。
  - `trimToLimits()` 不淘汰置顶条目（Maccy 语义：pin 即"永久保留"）。
  - `clear()` 只清未置顶；增加 `clearAll()` 连置顶一起清（菜单"清空历史…"按住 ⌥ 时走这个，菜单改动由 Kimi 做）。
  - 去重逻辑不变（同 payload 去重；若旧条目是置顶，去重后新条目继承 isPinned）。
- 测试：置顶排序、置顶免淘汰、clear 保留置顶、去重继承、旧 JSON 兼容。
- UI（Kimi）：卡片左上角 pin 标记 + `⌘P` 切换置顶。Codex 只需保证 `HistoryStore` API 就绪。

### T6 排除应用列表（已取消）

- 曾实现按来源应用停止记录，并直接放在菜单栏一级菜单中。
- 该入口难以理解，也缺少集中管理已排除应用的设置界面，因此功能和后台拦截均已移除。
- 若未来重新评估，必须通过独立设置页管理列表，不能恢复一级菜单中的动态入口。

### T7 历史保留策略（P1，现在是硬编码 200 条 / 25 MB）

- UserDefaults：`maximumEntryCount`（默认 200）、`maximumStorageMB`（默认 25）、`keepDays`（默认 0 = 不限时）。
- `HistoryStore.init` 读取这些值；`trimToLimits()` 增加按 `createdAt` 超龄清理（keepDays > 0 时）。
- 测试：自定义上限生效、超龄条目被清理、默认行为与现状一致。
- UI（设置入口）后续轮次再说，本轮只做机制。

### T8 QuickLook 预览（P2，缩略图的天然补充）

- Codex 部分：无（QLPreviewPanel 接线在窗口控制器里，Kimi 做）。Codex 只需保证 `ClipboardEntry` 不变。
- Kimi 部分：`Space` 触发 `QLPreviewPanel` 预览所选条目（图片大图、文本全文、文件预览）。

### T9 图片 OCR 搜索（P2 候选，差异化亮点，先不做）

- 方向：图片捕获后在串行后台队列用 Vision `VNRecognizeTextRequest` 识别，`ClipboardEntry` 增加 `recognizedText: String?`（Optional 红线），`searchableText` 拼接识别结果。
- 性能约束：识别前先降采样（复用 ThumbnailProvider 的思路），同一时刻只跑一个识别任务。
- 本轮不动，等第 2 轮稳定后再议。

## 7. 竞品调研备忘（2026-08）

- **Maccy**（开源标杆）：`⌥P` 置顶（置顶不受清空影响）、`⌥⇧⏎` 纯文本粘贴、忽略自定义 pasteboard 类型、⌥点击菜单暂停记录、"忽略下一次复制"、可调轮询间隔。其置顶/清空语义直接照搬到 T5。
- **Paste**：pinboards 多层收藏、iCloud 同步、AI 工具集成——都不适合 cpsmart 的定位，只借鉴"置顶"这一层。
- **Pastebot 3**：文本变换 filter（含 shell script）、Stacks 顺序粘贴队列、Shortcuts 集成、应用黑名单。应用黑名单因缺少合适的设置入口未采用；Stacks/顺序粘贴是 CleanClip 同款"杀手锏"，实现成本高（队列状态机 + 依次自动粘贴），列入观察，暂不做。
- **CleanClip**：粘贴队列、跟随前台 App 筛选（数据我们已有，`sourceAppBundleID` 筛选成本极低，第 3 轮候选）、拼音搜索、收藏夹。
- **Raycast**：pin、Paste as Plain Text。注意：cpsmart 捕获时只取 `.string`，文本天然无格式，"纯文本粘贴"对我们无意义，不做。
- **Paste It / Deck**：Vision OCR 图片搜索是主打卖点，本地运行不破坏隐私承诺 → T9。
- **pesty**（Paste 的开源仿品）：按来源应用着色的卡片头部色带，视觉上很直观，可作后续 UI 演进的参考。
- 快捷键提醒：Maccy 用 `⌘数字` 直接粘贴第 N 条——我们的 `⌘1–4` 已分配给类型筛选，若要支持数字键快贴，需要重新规划键位，两个功能二选一（当前维持筛选）。
